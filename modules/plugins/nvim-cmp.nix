{
    vim.autocomplete.nvim-cmp = {
        enable = true;
        mappings = {
            # select [n]ext and [p]revious suggestions
            next = "<C-n>";
            previous = "<C-p>";
            # scroll [b]ack and [f]orward
            scrollDocsUp = "<C-b>";
            scrollDocsDown = "<C-f>";
        };
    };
}
